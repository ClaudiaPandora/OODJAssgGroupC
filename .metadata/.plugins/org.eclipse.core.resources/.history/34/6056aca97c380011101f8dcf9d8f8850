package ui.common;

import models.User;
import javax.swing.*;
import java.awt.*;
import java.awt.geom.RoundRectangle2D;

public class HeaderPanel extends JPanel {
    
    private User currentUser;
    private Color NAVY_BLUE = new Color(0, 0, 128);
    private Color YELLOW = new Color(252, 202, 12);
    
    public HeaderPanel(User user) {
        this.currentUser = user;
        setBackground(NAVY_BLUE);
        setBorder(BorderFactory.createMatteBorder(0, 0, 2, 0, Color.BLACK));
        setLayout(new BorderLayout());
        setPreferredSize(new Dimension(0, 75));
        
        initializeComponents();
    }
    
    private void initializeComponents() {
        // Left panel with logo and title
        JPanel leftPanel = new JPanel(new FlowLayout(FlowLayout.LEFT, 20, 0));
        leftPanel.setOpaque(false);
        
        // Car logo
        CarLogoPanel carLogo = new CarLogoPanel();
        carLogo.setPreferredSize(new Dimension(50, 50));
        leftPanel.add(carLogo);
        
        // Title panel
        JPanel titlePanel = new JPanel();
        titlePanel.setLayout(new BoxLayout(titlePanel, BoxLayout.Y_AXIS));
        titlePanel.setOpaque(false);
        
        JLabel titleLabel = new JLabel("APU-ASC SUPPORT SYSTEM");
        titleLabel.setAlignmentX(Component.LEFT_ALIGNMENT);
        titleLabel.setFont(new Font("Segoe UI", Font.BOLD, 20));
        titleLabel.setForeground(Color.WHITE);
        titleLabel.setAlignmentX(Component.LEFT_ALIGNMENT);
        titlePanel.add(titleLabel);
        
        titlePanel.add(Box.createVerticalStrut(2));
        
        // Welcome panel 
        JPanel welcomePanel = new JPanel();
        welcomePanel.setLayout(new BoxLayout(welcomePanel, BoxLayout.X_AXIS));
//        welcomePanel.setOpaque(false);
        welcomePanel.setAlignmentX(Component.LEFT_ALIGNMENT);
        
        JLabel welcomeLabel = new JLabel("Welcome, ");
        welcomeLabel.setFont(new Font("Segoe UI", Font.PLAIN, 13));
        welcomeLabel.setForeground(Color.WHITE);
        welcomePanel.add(welcomeLabel);
        
        JLabel nameLabel = new JLabel(currentUser.getFullName());
        nameLabel.setFont(new Font("Segoe UI", Font.BOLD, 13));
        nameLabel.setForeground(Color.WHITE);
        welcomePanel.add(nameLabel);
        
        JLabel roleLabel = new JLabel(" (" + currentUser.getRoleDisplayName() + ")");
        roleLabel.setFont(new Font("Segoe UI", Font.PLAIN, 11));
        roleLabel.setForeground(YELLOW);
        welcomePanel.add(roleLabel);
        
        titlePanel.add(welcomePanel);
        leftPanel.add(titlePanel);
        
        add(leftPanel, BorderLayout.WEST);
    }
    
    // Car logo panel
    private class CarLogoPanel extends JPanel {
        public CarLogoPanel() {
            setOpaque(false);
        }
        
        @Override
        protected void paintComponent(Graphics g) {
            super.paintComponent(g);
            Graphics2D g2d = (Graphics2D) g;
            g2d.setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints.VALUE_ANTIALIAS_ON);
            
            int width = getWidth();
            int height = getHeight();
            
            // Yellow background
            g2d.setColor(YELLOW);
            g2d.fill(new RoundRectangle2D.Double(3, 3, width - 6, height - 6, 10, 10));
            
            // Border
            g2d.setColor(Color.BLACK);
            g2d.setStroke(new BasicStroke(2f));
            g2d.draw(new RoundRectangle2D.Double(3, 3, width - 6, height - 6, 10, 10));
            
            // Car body
            g2d.setColor(NAVY_BLUE);
            g2d.fillRoundRect(12, height/2 - 2, 26, 12, 6, 6);
            
            // Car top
            g2d.fillRoundRect(18, height/2 - 8, 13, 8, 4, 4);
            
            // Windows
            g2d.setColor(new Color(200, 220, 255));
            g2d.fillRoundRect(20, height/2 - 6, 5, 5, 2, 2);
            g2d.fillRoundRect(27, height/2 - 6, 3, 5, 2, 2);
            
            // Wheels
            g2d.setColor(Color.BLACK);
            g2d.fillOval(15, height/2 + 6, 7, 7);
            g2d.fillOval(30, height/2 + 6, 7, 7);
            
            // Wheel hubs
            g2d.setColor(Color.LIGHT_GRAY);
            g2d.fillOval(17, height/2 + 8, 3, 3);
            g2d.fillOval(32, height/2 + 8, 3, 3);
            
            // Headlight
            g2d.setColor(YELLOW);
            g2d.fillOval(36, height/2 + 1, 3, 3);
            
            // Taillight
            g2d.setColor(Color.RED);
            g2d.fillOval(10, height/2 + 1, 3, 3);
        }
    }
}