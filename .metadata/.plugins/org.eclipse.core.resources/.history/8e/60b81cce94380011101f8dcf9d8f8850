package ui.auth;

import models.User;
import services.AuthenticationService;
import ui.common.BaseFrame;
import ui.dashboard.DashboardFrame;

import javax.swing.*;
import java.awt.*;
import java.awt.event.MouseAdapter;
import java.awt.event.MouseEvent;
import java.awt.geom.RoundRectangle2D;

public class LoginFrame extends BaseFrame {
    
    private JTextField usernameField;
    private JPasswordField passwordField;
    private AuthenticationService authService;
    
    public LoginFrame() {
        super();
        this.authService = new AuthenticationService();
        setTitle("APU-ASC Login");
        setSize(450, 600);
        setLocationRelativeTo(null);
        setResizable(false);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        
        initializeComponents();
        setupLayout();
        addEventHandlers();
    }
    
    @Override
    protected void initializeComponents() {
        usernameField = new JTextField(20);
        passwordField = new JPasswordField(20);
        
        usernameField.setFont(new Font("Segoe UI", Font.PLAIN, 14));
        passwordField.setFont(new Font("Segoe UI", Font.PLAIN, 14));
    }
    
    @Override
    protected void setupLayout() {
        // Main panel with navy background
        JPanel mainPanel = new JPanel(new BorderLayout());
        mainPanel.setBackground(NAVY_BLUE);
        
        // White card panel
        JPanel cardPanel = new JPanel();
        cardPanel.setLayout(new BoxLayout(cardPanel, BoxLayout.Y_AXIS));
        cardPanel.setBackground(Color.WHITE);
        cardPanel.setBorder(BorderFactory.createCompoundBorder(
            BorderFactory.createLineBorder(BORDER_LIGHT, 2),
            BorderFactory.createLineBorder(BORDER_DARK, 2)
        ));
        
        // ===== HEADER =====
        JPanel headerPanel = new JPanel();
        headerPanel.setBackground(NAVY_BLUE);
        headerPanel.setPreferredSize(new Dimension(450, 120));
        headerPanel.setMaximumSize(new Dimension(450, 120));
        headerPanel.setLayout(new BoxLayout(headerPanel, BoxLayout.Y_AXIS));
        headerPanel.setBorder(BorderFactory.createMatteBorder(0, 0, 2, 0, Color.BLACK));
        
        // Car logo panel
        JPanel logoPanel = new JPanel(new FlowLayout(FlowLayout.CENTER));
        logoPanel.setOpaque(false);
        
        CarLogoPanel carLogo = new CarLogoPanel();
        carLogo.setPreferredSize(new Dimension(60, 60));
        logoPanel.add(carLogo);
        
        JLabel titleLabel = new JLabel("APU-ASC SUPPORT SYSTEM");
        titleLabel.setFont(new Font("Segoe UI", Font.BOLD, 18));
        titleLabel.setForeground(Color.WHITE);
        titleLabel.setAlignmentX(Component.CENTER_ALIGNMENT);
        
        JLabel subtitleLabel = new JLabel("Support That Moves You Forward");
        subtitleLabel.setFont(new Font("Segoe UI", Font.BOLD, 10));
        subtitleLabel.setForeground(YELLOW);
        subtitleLabel.setAlignmentX(Component.CENTER_ALIGNMENT);
        
        headerPanel.add(Box.createVerticalStrut(12));
        headerPanel.add(logoPanel);
        headerPanel.add(Box.createVerticalStrut(8));
        headerPanel.add(titleLabel);
        headerPanel.add(Box.createVerticalStrut(3));
        headerPanel.add(subtitleLabel);
        
        cardPanel.add(headerPanel);
        
        // ===== FORM =====
        JPanel formPanel = new JPanel();
        formPanel.setLayout(new BoxLayout(formPanel, BoxLayout.Y_AXIS));
        formPanel.setBackground(Color.WHITE);
        formPanel.setBorder(BorderFactory.createEmptyBorder(30, 40, 25, 40));
        
        // Username section with icon
        JPanel usernameLabelPanel = new JPanel(new FlowLayout(FlowLayout.LEFT, 5, 0));
        usernameLabelPanel.setBackground(Color.WHITE);
        
        // Create user icon
        JLabel userIconLabel = new JLabel() {
            @Override
            protected void paintComponent(Graphics g) {
                super.paintComponent(g);
                Graphics2D g2d = (Graphics2D) g;
                g2d.setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints.VALUE_ANTIALIAS_ON);
                
                // Draw person icon
                g2d.setColor(new Color(100, 100, 100));
                // Head
                g2d.fillOval(5, 2, 6, 6);
                // Body
                g2d.fillOval(3, 9, 10, 8);
            }
        };
        userIconLabel.setPreferredSize(new Dimension(16, 18));
        userIconLabel.setBackground(Color.WHITE);
        usernameLabelPanel.add(userIconLabel);
        
        JLabel userLabel = new JLabel("USERNAME");
        userLabel.setFont(new Font("Segoe UI", Font.BOLD, 10));
        userLabel.setForeground(new Color(100, 100, 100));
        usernameLabelPanel.add(userLabel);
        
        formPanel.add(usernameLabelPanel);
        formPanel.add(Box.createVerticalStrut(5));
        
        JPanel usernameFieldPanel = createInsetPanel(usernameField, 370, 40);
        formPanel.add(usernameFieldPanel);
        formPanel.add(Box.createVerticalStrut(18));
        
        // Password section with icon
        JPanel passwordLabelPanel = new JPanel(new FlowLayout(FlowLayout.LEFT, 5, 0));
        passwordLabelPanel.setBackground(Color.WHITE);
        
        // Create lock icon
        JLabel lockIconLabel = new JLabel() {
            @Override
            protected void paintComponent(Graphics g) {
                super.paintComponent(g);
                Graphics2D g2d = (Graphics2D) g;
                g2d.setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints.VALUE_ANTIALIAS_ON);
                
                // Draw lock icon
                g2d.setColor(new Color(100, 100, 100));
                // Shackle
                g2d.setStroke(new BasicStroke(2));
                g2d.drawArc(4, 2, 8, 8, 0, -180);
                // Body
                g2d.fillRoundRect(3, 7, 10, 9, 2, 2);
                // Keyhole
                g2d.setColor(Color.WHITE);
                g2d.fillOval(6, 11, 4, 3);
            }
        };
        lockIconLabel.setPreferredSize(new Dimension(16, 18));
        lockIconLabel.setBackground(Color.WHITE);
        passwordLabelPanel.add(lockIconLabel);
        
        JLabel passLabel = new JLabel("PASSWORD");
        passLabel.setFont(new Font("Segoe UI", Font.BOLD, 10));
        passLabel.setForeground(new Color(100, 100, 100));
        passwordLabelPanel.add(passLabel);
        
        formPanel.add(passwordLabelPanel);
        formPanel.add(Box.createVerticalStrut(5));
        
        JPanel passwordFieldPanel = createInsetPanel(passwordField, 370, 40);
        formPanel.add(passwordFieldPanel);
        formPanel.add(Box.createVerticalStrut(25));
        
        // Login button
        JButton loginButton = new JButton("LOGIN");
        loginButton.setFont(new Font("Segoe UI", Font.BOLD, 12));
        loginButton.setBackground(NAVY_BLUE);
        loginButton.setForeground(Color.WHITE);
        loginButton.setFocusPainted(false);
        loginButton.setBorderPainted(false);
        loginButton.setOpaque(true);
        loginButton.setContentAreaFilled(true);
        loginButton.setCursor(new Cursor(Cursor.HAND_CURSOR));
        loginButton.addActionListener(e -> handleLogin());
        
        formPanel.add(createOutsetPanel(loginButton, 370, 45));
        formPanel.add(Box.createVerticalStrut(15));
        
        // ===== DEMO  =====
        JPanel dividerPanel = new JPanel(new FlowLayout(FlowLayout.CENTER, 10, 0));
        dividerPanel.setBackground(Color.WHITE);
        dividerPanel.setMaximumSize(new Dimension(370, 25));
        
        JSeparator leftSep = new JSeparator(SwingConstants.HORIZONTAL);
        leftSep.setPreferredSize(new Dimension(80, 1));
        leftSep.setForeground(new Color(200, 200, 200));
        dividerPanel.add(leftSep);
        
        JLabel demoLabel = new JLabel("QUICK ACCESS DEMO");
        demoLabel.setFont(new Font("Segoe UI", Font.BOLD, 9));
        demoLabel.setForeground(new Color(150, 150, 150));
        dividerPanel.add(demoLabel);
        
        JSeparator rightSep = new JSeparator(SwingConstants.HORIZONTAL);
        rightSep.setPreferredSize(new Dimension(80, 1));
        rightSep.setForeground(new Color(200, 200, 200));
        dividerPanel.add(rightSep);
        
        formPanel.add(dividerPanel);
        formPanel.add(Box.createVerticalStrut(12));
        
        // Demo buttons
        JPanel demoPanel = new JPanel(new GridLayout(2, 2, 10, 10));        
        demoPanel.setBackground(Color.WHITE);
        demoPanel.setMaximumSize(new Dimension(370, 140));
        demoPanel.setPreferredSize(new Dimension(370, 140));
        demoPanel.setAlignmentX(Component.CENTER_ALIGNMENT);
        
        String[] roles = {"MANAGER", "COUNTER STAFF", "TECHNICIAN", "CUSTOMER"};
        String[] demoUsers = {"manager1", "counter1", "tech1", "cust1"};
        
        for (int i = 0; i < roles.length; i++) {
            JButton btn = createDemoButton(roles[i], demoUsers[i]);
            demoPanel.add(btn);
        }
        
        formPanel.add(demoPanel);
        formPanel.add(Box.createVerticalStrut(20));
               
        cardPanel.add(formPanel);
        
        // ===== FOOTER =====
        JPanel footerPanel = new JPanel(new FlowLayout(FlowLayout.CENTER));
        footerPanel.setBackground(new Color(245, 245, 245));
        footerPanel.setBorder(BorderFactory.createMatteBorder(1, 0, 0, 0, BORDER_DARK));
        footerPanel.setPreferredSize(new Dimension(450, 38));
        footerPanel.setMaximumSize(new Dimension(450, 38));
        
        // Shield icon
        JLabel shieldLabel = new JLabel() {
            @Override
            protected void paintComponent(Graphics g) {
                super.paintComponent(g);
                Graphics2D g2d = (Graphics2D) g;
                g2d.setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints.VALUE_ANTIALIAS_ON);
                
                // Draw shield icon
                g2d.setColor(new Color(120, 120, 120));
                int[] xPoints = {8, 16, 16, 8, 0, 0};
                int[] yPoints = {0, 3, 12, 16, 12, 3};
                g2d.fillPolygon(xPoints, yPoints, 6);
            }
        };
        shieldLabel.setPreferredSize(new Dimension(18, 18));
        shieldLabel.setBackground(new Color(245, 245, 245));
        
        JLabel footerLabel = new JLabel("OODJ Assignment Group C");
        footerLabel.setFont(new Font("Segoe UI", Font.BOLD, 10));
        footerLabel.setForeground(new Color(120, 120, 120));
        
        footerPanel.add(shieldLabel);
        footerPanel.add(Box.createHorizontalStrut(5));
        footerPanel.add(footerLabel);
        
        cardPanel.add(footerPanel);
        
        mainPanel.add(cardPanel, BorderLayout.CENTER);
        
        add(mainPanel);
    }
    
    private JPanel createInsetPanel(JComponent field, int width, int height) {
        JPanel panel = new JPanel(new BorderLayout());
        panel.setBackground(Color.WHITE);
        panel.setMaximumSize(new Dimension(width, height));
        panel.setPreferredSize(new Dimension(width, height));
        panel.setBorder(BorderFactory.createCompoundBorder(
            BorderFactory.createLineBorder(BORDER_DARK, 2),
            BorderFactory.createLineBorder(BORDER_LIGHT, 2)
        ));
        
        field.setBorder(BorderFactory.createEmptyBorder(8, 12, 8, 12));
        panel.add(field, BorderLayout.CENTER);
        return panel;
    }
    
    private JPanel createOutsetPanel(JComponent button, int width, int height) {
        JPanel panel = new JPanel(new BorderLayout());
        panel.setBackground(NAVY_BLUE);
        panel.setMaximumSize(new Dimension(width, height));
        panel.setPreferredSize(new Dimension(width, height));
        panel.setBorder(BorderFactory.createCompoundBorder(
            BorderFactory.createLineBorder(BORDER_LIGHT, 2),
            BorderFactory.createLineBorder(BORDER_DARK, 2)
        ));
        panel.add(button, BorderLayout.CENTER);
        return panel;
    }
    
    private JButton createDemoButton(String text, String username) {
        JButton btn = new JButton(text);
        btn.setFont(new Font("Segoe UI", Font.BOLD, 12));
        btn.setPreferredSize(new Dimension(170, 45));
        btn.setFocusPainted(false);
        btn.setBackground(new Color(240, 240, 240));
        btn.setForeground(Color.BLACK);
        btn.setBorder(BorderFactory.createCompoundBorder(
            BorderFactory.createLineBorder(BORDER_LIGHT, 1),
            BorderFactory.createLineBorder(BORDER_DARK, 1)
        ));
        btn.setCursor(new Cursor(Cursor.HAND_CURSOR));
        btn.addActionListener(e -> {
            usernameField.setText(username);
            passwordField.setText("password");
        });
        
        btn.addMouseListener(new MouseAdapter() {
            @Override
            public void mouseEntered(MouseEvent e) {
                btn.setBackground(new Color(230, 230, 230));
            }
            
            @Override
            public void mouseExited(MouseEvent e) {
                btn.setBackground(new Color(245, 245, 245));
            }
            
            @Override
            public void mousePressed(MouseEvent e) {
                btn.setBorder(BorderFactory.createCompoundBorder(
                    BorderFactory.createLineBorder(BORDER_DARK, 1),
                    BorderFactory.createLineBorder(BORDER_LIGHT, 1)
                ));
            }
            
            @Override
            public void mouseReleased(MouseEvent e) {
                btn.setBorder(BorderFactory.createCompoundBorder(
                    BorderFactory.createLineBorder(BORDER_LIGHT, 1),
                    BorderFactory.createLineBorder(BORDER_DARK, 1)
                ));
            }
        });
        
        return btn;
    }
    
    @Override
    protected void addEventHandlers() {
        passwordField.addActionListener(e -> handleLogin());
    }
    
    private void handleLogin() {
        String username = usernameField.getText().trim();
        String password = new String(passwordField.getPassword());
        
        if (username.isEmpty() || password.isEmpty()) {
            JOptionPane.showMessageDialog(this, 
                "Please enter username and password.", 
                "Login Error", 
                JOptionPane.ERROR_MESSAGE);
            return;
        }
        
        try {
            User user = authService.login(username, password);
            
            // Create and show dashboard on EDT
            SwingUtilities.invokeLater(() -> {
                DashboardFrame dashboard = new DashboardFrame(user);
                dashboard.setVisible(true);
            });
            
            // Dispose login frame
            this.dispose();
            
        } catch (Exception e) {
            JOptionPane.showMessageDialog(this, 
                "Invalid credentials. Please try again.", 
                "Login Error", 
                JOptionPane.ERROR_MESSAGE);
        }
    }
    
    // ===== CUSTOM CAR LOGO PANEL =====
    private class CarLogoPanel extends JPanel {
        public CarLogoPanel() {
            setOpaque(false);
        }
        
        @Override
        protected void paintComponent(Graphics g) {
            super.paintComponent(g);
            Graphics2D g2d = (Graphics2D) g;
            g2d.setRenderingHint(RenderingHints.KEY_ANTIALIASING, RenderingHints.VALUE_ANTIALIAS_ON);
            
            int width = getWidth();
            int height = getHeight();

            // ✅ SAFE PADDING (prevent cutoff)
            int padding = 6;
            int x = padding;
            int y = padding;
            int w = width - (padding * 2);
            int h = height - (padding * 2);

            // Shadow
            g2d.setColor(new Color(0, 0, 0, 30));
            g2d.fill(new RoundRectangle2D.Double(x + 2, y + 4, w, h, 12, 12));

            // Background
            g2d.setColor(YELLOW);
            g2d.fill(new RoundRectangle2D.Double(x, y, w, h, 12, 12));

            // Border
            g2d.setColor(NAVY_BLUE);
            g2d.setStroke(new BasicStroke(2.5f));
            g2d.draw(new RoundRectangle2D.Double(x, y, w, h, 12, 12));

            // ==== SCALE CAR RELATIVE TO SAFE AREA ====
            int carBaseX = x + 8;
            int carBaseY = y + h / 2;

            // Car body
            g2d.setColor(NAVY_BLUE);
            g2d.fillRoundRect(carBaseX, carBaseY - 6, 28, 12, 8, 8);

            // Cabin
            GradientPaint cabinPaint = new GradientPaint(
                carBaseX + 4, carBaseY - 14,
                NAVY_BLUE,
                carBaseX + 18, carBaseY - 14,
                new Color(0, 0, 180)
            );
            g2d.setPaint(cabinPaint);
            g2d.fillRoundRect(carBaseX + 6, carBaseY - 14, 14, 9, 6, 6);

            // Windows
            g2d.setColor(new Color(180, 210, 255));
            g2d.fillRoundRect(carBaseX + 8, carBaseY - 12, 5, 5, 3, 3);
            g2d.fillRoundRect(carBaseX + 14, carBaseY - 12, 3, 5, 2, 2);

            // Wheels
            g2d.setColor(Color.BLACK);
            g2d.fillOval(carBaseX + 3, carBaseY + 4, 8, 8);
            g2d.fillOval(carBaseX + 20, carBaseY + 4, 8, 8);

            // Rims
            g2d.setColor(new Color(80, 80, 80));
            g2d.fillOval(carBaseX + 5, carBaseY + 6, 4, 4);
            g2d.fillOval(carBaseX + 22, carBaseY + 6, 4, 4);

            // Headlight
            g2d.setColor(new Color(255, 255, 200));
            g2d.fillOval(carBaseX + 28, carBaseY - 1, 3, 3);

            // Taillight
            g2d.setColor(Color.RED);
            g2d.fillOval(carBaseX - 2, carBaseY - 1, 3, 3);

            // Ground line (within safe area)
            g2d.setColor(new Color(0, 0, 0, 50));
            g2d.drawLine(carBaseX, carBaseY + 14, carBaseX + 32, carBaseY + 14);
        }
    }
}